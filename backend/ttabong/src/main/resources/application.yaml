
spring:
    application:
        name: ttabong
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        database-platform: org.hibernate.dialect.MySQL8Dialect
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true
    logging:
        file:
            name: logs/common.log
server:
    servlet:
        context-path: /api
    port: 8080

---
spring:
    config:
        activate:
            on-profile: default
    datasource:
        url: jdbc:mysql://localhost:3307/ddabong_db
        username: ddabong
        password: 707
    data:
        redis:
            host: localhost
            port: 6379
    rabbitmq:
        host: localhost
        port: 5672
        username: user
        password: password
    logging:
        level:
            org.springframework: DEBUG
        file:
            name: logs/local.log

---
spring:
    config:
        activate:
            on-profile: dev
    datasource:
        url: jdbc:mysql://mysql:3306/ddabong_db
        username: user
        password: password
    data:
        redis:
            host: redis
            port: 6379
    rabbitmq:
        host: rabbitmq
        port: 5672
    logging:
        level:
            org.springframework: DEBUG
        file:
            name: logs/dev.log

---
spring:
    config:
        activate:
            on-profile: prod
    datasource:
        url: jdbc:mysql_container://mysql:3306/ddabong_db
        username: prod_user
        password: prod_password
    data:
        redis:
            host: redis_container
            port: 6379
    rabbitmq:
        host: rabbitmq_container
        port: 5672
    jpa:
        hibernate:
            ddl-auto: none
    logging:
        level:
            org.springframework: INFO
        file:
            name: logs/prod.log
    management:
        endpoints:
            web:
                exposure:
                    include: health,info